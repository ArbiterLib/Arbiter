{
  "name": "arbiter-node-bindings",
  "version": "0.1.0",
  "description": "Node.js bindings for Arbiter",
  "repository": "https://github.com/jspahrsummers/Arbiter/tree/master/bindings/node",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "prebuild": "npm run make && npm run configure",
    "build": "node-gyp build",
    "configure": "node-gyp configure",
    "prepublish": "build",
    "make": "env make build --directory=../.."
  },
  "devDependencies": {
    "bindings": "^1.2.1",
    "node-gyp": "^3.4.0"
  }
}
